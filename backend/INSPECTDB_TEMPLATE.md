# This is an auto-generated Django model module.
# You'll have to do the following manually to finalize things:
#   * Rearrange models' order
#   * Make sure each model has one field with primary_key=True
#   * Make sure each ForeignKey and OneToOneField has `on_delete` set to the desired behavior
#   * Remove `managed = False` lines if you wish to allow Django to create the table
# Feel free to rename the models, but leave the Meta options alone.

# Generated by: inspectdb
# To use these models:
# 1. Review all fields match your database schema
# 2. Rename as needed following Django conventions
# 3. Copy to their respective apps:
#    - Usuario → apps/users/models.py
#    - Producto → apps/products/models.py
#    - Venta, DetalleVenta, Pago → apps/orders/models.py

from django.db import models


class Usuario(models.Model):
    """Modelo Usuario desde base de datos existente"""
    # Ajusta estos campos según tu esquema real
    # Ejemplo:
    # id = models.AutoField(primary_key=True)
    # nombre = models.CharField(max_length=100)
    # email = models.EmailField()
    # etc.
    
    class Meta:
        managed = False  # No permitas que Django maneje esta tabla
        db_table = 'usuario'


class Producto(models.Model):
    """Modelo Producto desde base de datos existente"""
    # Ajusta estos campos según tu esquema real
    
    class Meta:
        managed = False
        db_table = 'producto'


class Venta(models.Model):
    """Modelo Venta desde base de datos existente"""
    # Ajusta estos campos según tu esquema real
    
    class Meta:
        managed = False
        db_table = 'venta'


class DetalleVenta(models.Model):
    """Modelo DetalleVenta desde base de datos existente"""
    # Ajusta estos campos según tu esquema real
    
    class Meta:
        managed = False
        db_table = 'detalle_venta'


class Pago(models.Model):
    """Modelo Pago desde base de datos existente"""
    # Ajusta estos campos según tu esquema real
    
    class Meta:
        managed = False
        db_table = 'pago'
